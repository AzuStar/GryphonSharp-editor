 - node schema
 > "node-{id}-nt-{nt}-af-{flags}-rf-{flags}"
 - argument schema
 > "node-{id}-icid-{id}"
 - return schema
 > "node-{id}-ocid-{id}"

'node' - (meta)
{id} - id (value)
'nt' - node type (meta)
{nt} - node type (value)

'af' - arguments flags (meta)
'rf' - returns flags (meta)

'icid' - in connector id (meta)
'ocid' - out connector id (meta)
{flags} - flags (value)