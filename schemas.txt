 - node schema
 > "node-{id}-nt-{nt}-af-{flags}-rf-{flags}"
 - argument schema
 > "node-{id}-acid-{id}"
 - return schema
 > "node-{id}-rcid-{id}"

'node' - (meta)
{id} - id (value)
'nt' - node type (meta)
{nt} - node type (value)

'af' - arguments flags (meta)
'rf' - returns flags (meta)

'acid' - argument connector id (meta)
'rcid' - return connector id (meta)
{flags} - flags (value)